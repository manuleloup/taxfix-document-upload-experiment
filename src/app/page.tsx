export default function Home() {
  return (
    <main style={{ maxWidth: 640, margin: "0 auto", padding: "48px 24px" }}>
      <h1>Taxfix — Document Upload Onboarding</h1>
      <p>
        Next.js rebuild in progress. Layout and flow reference:{" "}
        <code>taxfix-no-onboarding.html</code>. Styling is pending real
        <code> @taxfix/ds-components</code> access — see{" "}
        <code>other_resources/DESIGN-SYSTEM.md</code>.
      </p>
    </main>
  );
}
